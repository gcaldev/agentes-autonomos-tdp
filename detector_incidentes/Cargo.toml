[package]
name = "detector_incidentes"
version = "0.1.0"
edition = "2021"

# See more keys and their definitions at https://doc.rust-lang.org/cargo/reference/manifest.html

[dependencies]
azure_vision_client = { path = "../azure_vision_client" }
